/**
 * 基础状态类
 * @author gxj
 */
class BaseState {
  /**
   * 基类事件处理
   * @param context
   * @param eventType
   * @param params
   */
  handleEvent(context, eventType, params = {}) { }
}

export default BaseState;